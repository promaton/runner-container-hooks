{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "name": "run k8s tests",
            "request": "launch",
            "program": "${workspaceFolder}/packages/k8s/node_modules/.bin/jest",
            "args": ["packages/k8s/tests", "--runInBand"],
            "cwd": "${workspaceFolder}/packages/k8s",
            "console": "integratedTerminal",
        }
    ]
}